using UnityEngine;

public class ScrollView : MonoBehaviour
{
    //[SerializeField] private ScrollView _view;

    //public void ResetView()
    //{
    //    _view.transform.position = new Vector3(_view.transform.position.x, 0, _view.transform.position.z);

    //    Debug.Log(_view.transform.position.y);
    //}
}
